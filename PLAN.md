# 巴别图书馆 · 产品与开发 Plan（V3 · 已实现）

> 状态：✅ 全部决策已确认，MVP 已按本方案实现。
>
> 修订记录：
> - V16：反向定位从 About 迁入首页「馆员索引」（与辩护书/今日之页等工具同区）；About 全部重写为纯概念页（这里有什么 / 定位而非寻找 / 天书与朝圣 / 技术附记）。
> - V15：**无限接龙**——`#/?chain=` 链接携带全部段落（JSON + base64url），每位馆员一段、归属保留；写完可「接着传下去」（自己这棒即时入链）或「完成接龙并定位」（N 段拼合定位，超一页自动分段）；旧 `?draft=` 链接兼容；每棒限 200 字、链长超 1200 字提示收尾；复制链路统一 execCommand 回退。另修复收录证布局：印章移至右上内框、二维码与坐标收入框内、坐标超宽自适应缩字号/截断（`scripts/check-ticket.mjs` 一键出图复查）。
> - V14：**打通传播闭环**——① 修复系统分享带图分支丢失链接（文案一律附 URL）；② 分享链接带 `src=share`，书页底部新增高辨识 CTA「也给我的话找一个地址」（接收者视角加强）；③ 合著重构：第一位馆员文字只读展示、第二位独立续写框、结果标记两位馆员、邀请优先系统面板且剪贴板失败亮出链接、前半句限 200 字；④ 主题筹码不再自动定位（填充+全选，用户改完自己定位）；⑤ reduced-motion 下结果仍自动入视；⑥ 收录证弹窗焦点陷阱、禁方向键翻页、aria-pressed、关闭归还焦点；⑦ e2e 经 CDP 优雅关闭浏览器再清理（根治 EBUSY 退出码）；⑧ Worker 改 env 配置 + `?preview=1` 调试，README 明确「默认私密分享 / 可选丰富预览」的隐私取舍。
> - V13：**社交仪式包装**——① 收录证三装帧（宇宙收录证/未来墓志铭/原始藏书票，含二维码与"写下你的那一句"行动文案，`qrcode` 依赖）；② 主按钮改为「分享这句话」（优先 `navigator.share` 系统面板、可带图，不支持回退复制），次要动作与"更多"菜单分层；③ 主题人格入口（未来墓志铭/从未说出口的话/十年后的自己，按日轮换）；④ 合著接写（`#/?draft=` 链接携带前半句，朋友续写后定位合著句）；⑤ 揭示动画首次完整、后续 0.6s、可点击跳过、支持 prefers-reduced-motion 与 aria-live；⑥ 无障碍补丁（输入框标签、focus-visible、弹窗 role/aria/Esc/焦点、移动端 44px、暗色主按钮对比度）；⑦ 备用件 `workers/share.ts`（Cloudflare Worker 动态 OG 预览，部署后启用）。
> - V12：**体验收紧（按外部评审）**——⓪ 前置：短链接补完（翻页链/书籍页/页格改用「配方+delta」相对短链接，名著章节改 `/classic/书/章` 语义短路由，长链接仅剩整页自定义文字的规范地址）。① 首屏极简：只留一句话引言、输入框、「定位这句话」与弱化的「随意翻开一页」；语言池/限定文本/上传收进「更多定位方式」，更名为"页面书写体系/限定字符集"。② 单结果制：一次只定位一页，坐标逐层揭示（馆→层→室→架→册→页→找到了），「在图书馆的另一处寻找同一句话」替代"换一批"（取代 V3 的 10 条结果制）。③ Enter 恢复换行，Ctrl/Cmd+Enter 提交。④ **路由全部版本化 /v1/**：codec.ts 顶部冻结 v1 契约（字符集/页长/常数/配方算法/语言池/编码），未来变更只能以 /v2 引入，旧未版本化链接 301 重定向。⑤ 阅读页默认聚焦原句且只显示上下文（±2 行），完整书页折叠。⑥ 新增 /v1/t/ 文字页链接（反向定位只携带文字本身，短文本链接从万级字符降到百级）。⑦ 辩护书/今日之页/名著移入「馆员索引」，名著区改称"馆员已经标记的几页"（首页示三）；辩护书支持定位用户自写辩词。⑧ 藏书票图卡（canvas 生成可下载 PNG）、站点级 og.png、我的藏书夹（localStorage）、真目录隐藏彩蛋（输入球体句，编号与装帧异常，无任何提示）。⑨ 字体栈优化（Georgia 优先于各平台宋体）。
> - V11：**名著「一本书在一起」**——名著页默认改为「连读全书」（整书连续顺读，每章附"馆中此页"入口），「馆中目录」为第二模式；从名著进入的阅读页带章际导航链（← 上一章 / 第 N 章 / 下一章 →，键盘 ←/→ 同步按章翻页）。设计说明：多页书籍在馆中几乎必然不以连续地址存在（页空间穷尽、多页序列空间远远更大），"书"在馆中的诚实形态是一条路径——章际导航即此路径。另修复：About 文案整体重写优化；清理 e2e 僵尸浏览器进程导致的回归超时。
> - V10：**原作氛围强化**——①「寻找我的辩护书」（原作中馆员毕生寻找为自己一生辩护的书卷；输入名字即定位到必然存在的那一页）；② 朝圣计数（无目的翻页累计"你已途经 N 页无意义的文字"）；③ 镇页箴言（阅读页底部，按地址确定、同一页永远同一句）；④ 首页轮换博尔赫斯原句三则、"万有清单"（馆中藏有……你死亡的真实记录……一切）、检索结果页点破句；⑤ 错误页文案改为"假目录所虚构的所在"。
> - V9：**馆藏名著**——首页新增名著区：《道德经》（王弼本 81 章）、《孙子兵法》（13 篇）、《论语·学而第一》（16 章）、《唐诗十二首》、《莎士比亚十四行诗》（5 首选）。文本均为公版（中文维基文库 / Project Gutenberg），由 `scripts/build-classics.mjs` 抓取清洗生成 `src/classics/books.ts`；每章对应馆中真实一页，不足一页以空白补足（`fullPageAddress`），点击章节时实时算地址进规范长链接。另修复：About 反向定位不接受多行文本（换行改为自动忽略）；Book 页在 base64url 改造后仍按 hex 解析导致"这本书不属于本馆"。
> - V8：**短链接**——乱码填充改为种子确定性随机流（splitmix64，属永久契约），检索结果与随机漫游改用「配方」短链接（`#/s/…`，种子+位置+池掩码+限定文本+检索词，通常 50–100 字符），打开时重建整页再算真实坐标；规范长链接编码由 hex 改为 base64url（再短 1/3，约 1.15 万字符），书籍翻页等任意内容页仍用规范链接。超长检索词在配方反而更长时自动回退规范链接。注意：规范链接格式再次变更，旧链接失效。
> - V7：体验优化包——① 限定文本填充（乱码只使用给定文字的字符，优先于语言池）；② 首屏例句引导；③「只看原句」聚焦模式；④ 复制分享文案；⑤ 键盘 ←/→ 翻页；⑥ 语言池/限定文本/主题本地记忆；⑦ 暗色模式；⑧ 打印藏书票样式；⑨ 动态标题；⑩ About 页反向定位工具（整页文字 → 坐标）；⑪ 站点级 OG 标签；⑫ PWA（manifest + Service Worker 离线访问）。
> - V6：新增**乱码填充语言池**——首页复选框（汉字/拉丁/假名/韩文/西里尔/阿拉伯文/数字标点/emoji 符号），只影响检索结果的填充字符来源，不限制可检索的文字；全选时保持全字符集均匀。随意翻阅与分段定位同样遵循选择。
> - V5：每页容量 2000 → **4000 字**（50×80）；新增**长文本支持**——上传文本文件（UTF-8/GBK）或粘贴超长文本时自动按行分段，逐段定位到馆中真实书页，单次上限 100 段。注意：页容量变更使旧链接失效（编码空间重建）。顺带修复 `getRandomValues` 单次 64KB 上限的隐患。
> - V4：修复两个实测问题——① 报错对不可见字符显示为空白（改为展示 U+XXXX 码位，并提供"剔除这些符号"按钮；回车可直接检索）；② 相邻书页内容几乎不变（页内容改为页编号乘以全宽互素常数再取模，翻页即完全不同的页面，双射性质不变）。新增真实浏览器 e2e 回归脚本 `scripts/e2e-click.mjs`。
> - V3：确认全部决策项（D1–D8）；落实实现细节——Unicode 16.0 快照（154,826 个可打印字符）；路由改用 hash 模式（书页地址极长，放在 URL fragment 中不经服务器，永久链接在任意静态托管上都可靠）。
> - V2：字符集改为**全 Unicode 可打印字符**；每册加长为 **1000 页**。
> - V1：初稿。

---

## 1. 产品定位

**一句话**：一座包含"所有可能文本"的无限图书馆——你输入的任何一句话，都早已写在某本书的某一页上。

**概念来源**：博尔赫斯《巴别图书馆》。图书馆不"存储"任何书页，而是由一套确定性规则生成：每一页的内容由它的"地址"唯一决定，因此它是无限的、永恒的、可复现的。

**MVP 目标**：让用户在 30 秒内完成第一次"检索 → 打开书页 → 读到自己的那句话"的完整体验，并感受到震撼与荒谬感。

---

## 2. 核心体验

### 2.1 主用户旅程

1. 进入首页，看到一段简短的博尔赫斯式引言 + 一个检索框。
2. 输入任意一句话（例如"我今天中午吃了火锅"），点击检索。
3. 返回约 10 个"书页结果"：每条显示一个书页片段，其中**你的原句被高亮**，嵌在大段看似无意义的文字之中。
4. 点击任一结果，进入书页阅读页：看到完整一页，原句高亮，页面顶部显示这页在图书馆中的**完整地址**（如：第 8472 馆 · 第 3 层 · 第 2 室 · 第 5 架 · 第 17 册 · 第 291 页）。
5. 用户可以：翻上一页/下一页、查看这本书的其余页面、复制这页的永久链接分享给别人——任何人打开链接看到的都是同一页同样的内容。

### 2.2 体验的"魔法点"

- **不是搜索，是定位**：网站没有去"找"这句话，而是算出这句话必然所在的坐标。这是与原作文献最契合的表达，关于页会点破这一点。
- **永久可复现**：同一地址永远是同一页，不依赖数据库，链接永久有效。
- **无限但有序**：每一页都有唯一、层级化的地址，给人"这座图书馆真实存在"的错觉。
- **万有文字**：全 Unicode 字符集意味着图书馆里也有用古埃及圣书体、楔形文字、emoji 写成的"书"——绝大多数页面看起来像外星典籍。

---

## 3. MVP 范围

### 做

- 检索（输入任意文本 → 返回约 10 个包含该文本的书页；超过一页自动分段定位）
- 文本文件上传（.txt/.md 等，UTF-8/GBK 自动识别，逐段定位，单次上限 100 段）
- 书页阅读（完整页、高亮、地址、翻页）
- 书籍浏览（查看某页所属的"书"，即同册的 1000 页）
- 随机漫游（随机打开一页）
- 永久链接（地址即 URL，可分享）
- 关于页（解释概念与实现原理）
- 移动端可读

### 不做（本版本明确排除）

- 登录/注册、收藏、历史记录
- 社区、评论、分享墙、积分
- 3D 场景、漫游动画（视觉仅用排版与质感营造氛围）
- 多语言界面（界面中文；检索字符集为全 Unicode 可打印字符，见 D1）
- 全文模糊搜索、语义搜索（与概念相悖，也不可能）
- 用户生成内容、自定义书页

---

## 4. 页面规划

共 4 个页面，单页应用（SPA）：

| 页面 | 路由 | 功能 |
|---|---|---|
| 首页 / 检索页 | `/` | 引言、检索框、检索结果列表（片段 + 高亮 + 地址 + "换一批"）、随机漫游入口 |
| 书页阅读页 | `/page/:address` | 完整页文本、检索词高亮（从 URL 参数带过来）、层级地址、上一页/下一页、跳转到所属书、复制链接 |
| 书籍页 | `/book/:bookAddress` | 该册 1000 页的页码列表（分页展示），点击进阅读页；显示该册的"书名"（地址编码的伪书名，纯氛围） |
| 关于页 | `/about` | 博尔赫斯原文节选、概念解释、"它是如何工作的"（用通俗语言讲清生成原理） |

---

## 5. 核心实现原理（重点）

整个网站**不存储任何文本**，核心是一个"页内容 ⇔ 页地址"的可逆映射 + 一个纯函数库。

### 5.1 字符集与页面规格（已确认：全 Unicode 可打印字符；书籍加长）

- 字符集 Σ = **Unicode 中所有"可打印"字符**：按码位升序排列，排除控制符（Cc）、格式符（Cf）、代理项（Cs）、私用区（Co）、未分配码位（Cn）与分行/分段符（Zl/Zp）。包含汉字、拉丁/西里尔/阿拉伯/假名等全部文字、标点、数学符号、emoji 等，Unicode 16.0 下共 **154,826** 个符号。
- **永久性的关键**：字符集固定为 **Unicode 16.0** 的快照，构建时生成一张**码位区间表**提交到代码仓库（735 个区间，仅数 KB）。运行时只读这张表，不随环境 Unicode 升级而变化——否则旧地址会"漂移"到别的页。
- 用户输入统一先做 **NFC 规范化**再校验，避免"é"这类字符的两种编码被视为不同文本。
- 每页固定 L 个字符（**4000 字**，50 字 × 80 行）；**每册 P = 1000 页**（原作 410 页的加长版，具体数值可再调，只是一个常量）。
- "所有可能的页"总数为 |Σ|^L ≈ 150000^2000——有限但超越可观测宇宙，永不重复、永不枯竭。

### 5.2 页内容 ⇔ 地址：一个双射

- 把一页的 L 个字符看作一个 **|Σ| 进制的 L 位大整数**：这就是这页的"页编号"（0 ~ |Σ|^L − 1）。
- 解码（地址 → 内容）：把大整数按 |Σ| 进制展开，每一位映射回字符。**生成一页只需一次大数转换，约毫秒级，无需任何数据库。**
- 编码（内容 → 地址）：反过来，把字符序列转成编号。
- 显示地址：把页编号按混合进制拆成层级坐标（馆 → 层 → 室 → 架 → 册 → 页），例如每册 1000 页、每架 32 册、每室 5 架、每层 4 室，馆数无限。用户看到的永远是这种"图书馆坐标"，而不是天文数字。
- 细节：对编号做一次可逆置换（乘一个与模数互素的大常数再取模），避免"全空白页 = 0 号地址"这类显眼的规律，让地址看起来均匀随机。

### 5.3 检索如何实现

这是整个产品最巧的一点——**检索即编码**：

1. 校验查询：先做 NFC 规范化；若含字符集外的字符（控制符、私用区字符、以及 ZWJ 拼接的组合 emoji 这类无法逐码位收录的序列），明确报出"本馆不收录这些符号"并列出它们。超过一页的文本自动按行分段（换行不收录于字符集，仅作边界），逐段定位，单次上限 100 段。
2. 生成第 i 个结果：把查询句放在一个随机起始位置，**前后用随机字符填满整页**，得到一整页"恰好包含原句"的合法文本。
3. 把这页文本编码成地址——这个地址就是"包含这句话的某一页"的真实坐标。
4. 换一批随机填充 = 换一批坐标，因此每次检索都能得到不同的 10 个结果，且每个结果都真实、永久有效。

> 关键性质：结果不是编出来的"假书页"——它就是图书馆里真实存在的那一页，链接永久有效，和从地址浏览到的完全一致。

### 5.4 随机漫游

随机生成一个合法页编号 → 解码成内容。全 Unicode 字符集下，随机页看起来像外星文字与失传典籍的混合（部分生僻码位无字体可显示，会呈现为"豆腐块"，这也符合图书馆的气质，关于页会说明）。关于页同时会点破：你检索到的那一页，是这座图书馆里仅有的、为你而 meaningful 的一页。

---

## 6. 技术方案

**结论：纯前端静态网站，零后端、零数据库。** 所有计算（大数编码/解码）都在浏览器里用原生 `BigInt` 完成。

| 项 | 选型 | 理由 |
|---|---|---|
| 框架 | Vite + Vue 3 + TypeScript | 4 个页面的 SPA，轻量够用；若要更极简可降为无框架 Vanilla TS（见 D6） |
| 核心算法 | 独立 TS 模块 `core/`，纯函数 | 可单测、与 UI 解耦；BigInt 原生支持，无需依赖 |
| 字符集数据 | 构建期脚本生成码位区间表（固定 Unicode 版本），提交仓库 | 仅数 KB；保证地址永久不漂移 |
| 路由 | vue-router（hash 模式） | 书页地址极长（数千字符），放 URL fragment 不经服务器，永久链接在任意静态托管上都可靠 |
| 样式 | 手写 CSS（羊皮纸/衬线字体/打字机质感的"图书馆"视觉） | 不引 UI 框架，风格独特且包体积极小 |
| 测试 | Vitest 单测覆盖 `core/` | 双射往返、检索结果必含原句、地址分层拆解 |
| 部署 | 静态托管（Cloudflare Pages / Vercel / GitHub Pages 任一） | 免费、全球可访问、构建即部署 |

**目录结构（预期）**

```
src/
  core/          # 纯函数核心：字符集表、编码/解码、地址分层、检索生成、随机页
  pages/         # Home / Page / Book / About
  router.ts
  main.ts
scripts/         # 字符集区间表生成脚本（构建期一次性运行，产物提交仓库）
tests/           # core 的单元测试
```

**性能预估**：字符→序号映射用预建 Map（约 15 万条，数 MB 内存）；单页生成（约 5.5 万位大整数的 2000 次进制拆解）< 20ms；检索 10 条 < 100ms；首屏体积 < 200KB。无服务端成本。

---

## 7. 开发步骤

1. **核心算法库**（地基，先行）：字符集区间表生成脚本（固定 Unicode 版本快照，产物提交仓库）、双射编码/解码、可逆置换、地址分层拆解、检索结果生成、随机页生成 + Vitest 单测全部通过。
2. **工程脚手架**：Vite + Vue + TS + 路由 + 部署流水线（推到仓库即可自动部署出预览链接）。
3. **检索页**：输入校验（NFC 规范化、非法字符提示、超长提示）、结果列表、高亮、换一批。
4. **阅读页**：完整页渲染、高亮、翻页、跳书、复制链接。
5. **书籍页 + 随机漫游**。
6. **关于页 + 视觉打磨**（桌面/移动端）。
7. **验收 + 正式上线**。

---

## 8. 验收标准

**功能正确性（核心算法，单测保障）**
- [ ] 任意页 → 编码成地址 → 解码回来，内容逐字一致（双射往返）
- [ ] 检索返回的每一页都**逐字包含**用户原句，且高亮位置正确
- [ ] 同一地址在任何设备、任何时间打开，内容完全一致（确定性）
- [ ] 地址分层拆解与重组一致（页编号 ⇔ 馆/层/室/架/册/页）
- [ ] 输入经 NFC 规范化后逐字保真；中/英/日/ emoji/生僻字等常见输入均可检索
- [ ] 字符集区间表固定（不随运行环境 Unicode 版本变化），旧地址永久可复现

**体验**
- [ ] 首次访问 30 秒内可完成"检索 → 打开书页"全流程
- [ ] 书页链接可直接分享，接收者打开看到同一页
- [ ] 移动端阅读排版舒适（字号、行距、行长合理）
- [ ] 检索框对非法字符、超长输入有明确的、符合图书馆世界观的提示

**工程**
- [ ] 首屏 < 200KB（gzip 前可略宽），检索/翻页无明显卡顿
- [ ] 核心模块单测全部通过
- [ ] 静态部署成功，自定义域名可后续接入

---

## 9. 需要你确认的产品决策

> 全部已确认。以下为实现时的最终口径（含落实细节）。

- **D1 · 检索字符集**：✅ 已确认——**全 Unicode 可打印字符**（Unicode 16.0 快照，154,826 个符号，含 emoji 与生僻字）。
  - 附带约束：控制符/格式符/私用区/未分配码位不收录，因此 ZWJ 拼接的组合 emoji（如"家庭"类组合）无法逐码位收录，检索时会明确报错；单码位 emoji 正常。
- **D2 · 书籍规格**：✅ 已确认加长——每册 **1000 页**；每页 **4000 字（50×80）**（V5 由 2000 字加大，以支持更长检索；页容量变更会使旧链接失效）。
- **D3 · 单次检索结果数**：✅ 已确认——**10 条 + "换一批"**（重新随机填充，理论上无穷无尽）。
- **D4 · 非法字符处理**：✅ 已确认——输入含字符集外的字符（控制符、私用区字符、ZWJ 组合 emoji 等）时**直接报出"本馆不收录这些符号"的提示并列出它们**，不做静默过滤。
- **D5 · 地址体系**：✅ 已确认——分层坐标（馆/层/室/架/册/页）作为对外展示，不暴露大整数原文；馆编号过长时首尾截断展示，完整编号收纳在可展开的详情里。
- **D6 · 技术形态**：✅ 已确认——**纯前端静态站**（零成本、零运维、永久链接天然成立）。后续如需统计/API 再议。
- **D7 · 是否需要"随机漫游"入口**：✅ 已确认——保留（首页"随意翻阅"按钮）。
- **D8 · 网站名称与视觉基调**：✅ 已确认——站名"巴别图书馆"，旧纸页 + 衬线字 + 朱砂点缀的克制文字质感，无 3D/动效炫技。

---

## 10. 后续版本展望（仅记录，本版不做）

- "逐字定位"（输入一页全文直接得地址）；按"馆"浏览的导航器
- 书签（本地 localStorage 收藏书页）、浏览历史
- 伪书名生成、多语言界面
- PWA 离线访问；开放 API
